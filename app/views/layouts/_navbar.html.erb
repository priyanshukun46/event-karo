<nav class="bg-white border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex items-center justify-between h-16">

      <!-- Logo -->
      <div>
        <%= link_to "Event Karo", root_path, class:"flex flex-shrink-0 font-medium text-3xl font-bold items-center " %>
      </div>

      <!-- Buttons auth  -->
      <div class="ml-6 flex items-center gap-4">
        <% if user_signed_in? %>
            <%= link_to "Log out", destroy_user_session_path, data: {"turbo-method": :delete }, class:"border border-black rounded-md py-2 px-6 hover:border-b-2 hover:border-r-4 hover:-ml-1 hover:-mt-1" %>
        <% else %>
            <%= link_to "Log in", new_user_session_path, class:"border border-black rounded-md py-2 px-6 hover:border-b-2 hover:border-r-4 hover:-ml-1 hover:-mt-1" %>
        <% end %>
      </div>

    </div>
  </div>
</nav>
